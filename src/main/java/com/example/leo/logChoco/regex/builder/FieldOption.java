package com.example.leo.logChoco.regex.builder;

/**
 * list for possible options for each format
 * */
public enum FieldOption {
    // can be empty,
    EMPTY,
    //max length
    MAXLENGTH,
    //min length,
    MINLENGTH,
    //date format
    DATEFORMAT,
    // user defined regular expression
    REGEX

}
